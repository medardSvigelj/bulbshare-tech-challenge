import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const DialogScreen = styled(Box)({
  width: "100%",
  height: "100vh",
  backgroundSize: "cover",
  display: "flex",
  justifyContent: "center",
});
